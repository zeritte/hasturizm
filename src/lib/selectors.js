export const departureLocationSelector = (state) => state.search.departureLocation;
export const arrivalLocationSelector = (state) => state.search.arrivalLocation;
export const previousSearchesSelector = (state) => state.search.previousSearches;
